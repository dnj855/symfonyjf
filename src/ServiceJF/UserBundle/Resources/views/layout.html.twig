{% extends '::layout.html.twig' %}

{% block header_padding %}
    <div class="py-4">
    {% endblock %}

        {% block body_title %}
        mon compte
        {% endblock %}

{% block body %}
{% if app.user %}
<div class="sub-nav">
        <div class="container">
            <ul class="nav justify-content-end">
            <li class="nav-item">
                    <a class="nav-link text-secondary active" href="{{ path('fos_user_change_password') }}">Modifier son mot de passe</a>
                </li>
                            <li class="nav-item">
                    <a class="nav-link text-secondary active" href="{{ path('fos_user_profile_edit') }}">Modifier mon compte</a>
                </li>
            </ul>
        </div>
    </div>
    {% endif %}
        <div class="container">
            <div class="row pt-4">
                <div class="col-sm-12 col-md-6 offset-md-6">
{% block fos_user_content %}
{% endblock fos_user_content %}
                </div>
            </div>
            </div>
{% endblock body %}