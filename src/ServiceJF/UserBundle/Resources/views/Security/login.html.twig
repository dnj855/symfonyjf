<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Service j&f: - Se connecter</title>
    <link rel="stylesheet" href="{{ asset('assets/css/custom.css') }}">
    <link rel="stylesheet" href="{{ asset('assets/css/design.css') }}">
    <script defer src="https://use.fontawesome.com/releases/v5.0.13/js/all.js"
            integrity="sha384-xymdQtn1n3lH2wcu0qhcdaOpQwyoarkgLVxC/wZ5q7h9gHtxICrpcaSUfygqZGOe"
            crossorigin="anonymous"></script>

</head>
<body>
<div id="header">
    <div class="container">
        <div class="row py-3 py-md-6 text-md-left text-center align-items-center">
            <div class="col-md-9 col-sm-12 d-flex flex-column">
                <h1 class="text-light">service j&f:</h1>
                <div class="h2 subtitle">se connecter</div>
            </div>
            <div class="col-md-3 col-sm-12">
                {% for key, messages in app.session.flashbag.all() %}
                    {% for message in messages %}
                        <div class="alert alert-{{ key }} alert-dismissible fade show">
                            {{ message|trans({}, 'FOSUserBundle') }}
                            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                <span aria-hidden="true" class="ml-auto">&times;</span>
                            </button>
                        </div>
                    {% endfor %}
                {% endfor %}
                {% if error %}
                    <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}
            </div>
        </div>
    </div>
</div>
<div id="body">
    <div class="container">
        <div class="row mt-4">
            <div class="col-12 col-md-6 order-md-2 mb-3 mb-md-0 offset-md-1">
                <form action="{{ path("fos_user_security_check") }}" method="post">
                    {% if csrf_token %}
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token }}"/>
                    {% endif %}
                    <div class="form-group mb-2">
                        <label for="username">Nom d'utilisateur :</label>
                        <input type="text" id="username" name="_username" value="{{ last_username }}"
                               required="required" class="form-control"/>
                    </div>
                    <div class="form-group mb-2">
                        <label for="password">Mot de passe :</label>
                        <input type="password" id="password" name="_password" required="required"
                               class="form-control"/>
                    </div>
                    <div class="form-check">
                        <input class="form-check-input" type="checkbox" id="remember_me" name="_remember_me"
                               value="on">
                        <label class="form-check-label" for="remember_me">
                            Se souvenir de moi
                        </label>
                    </div>
                    <div class="d-flex flex-row-reverse">
                        <input type="submit" id="_submit" name="_submit" value="Se connecter"
                               class="btn btn-primary text-light"/>
                    </div>
                </form>
                <div class="d-flex justify-content-between mt-1 small ">
                    <a href="{{ path('fos_user_resetting_request') }}">Mot de passe oublié</a>
                    <a href="{{ path('servicejf_migration_step1') }}">Première connexion sur le nouveau site</a>
                </div>
            </div>
            <div class="col-12 col-md-4 offset-md-1 order-md-1 mt-md-3">
                <div class="card border-primary card-body my-0 py-0">
                    <div class="ribbon-new"><span>S'inscrire</span></div>
                    <a href="{{ path('servicejf_jeudi_register') }}">
                        <div class="justify-content-center d-flex flex-column align-items-center">
                            <img height="200px"
                                 src="{{ asset('assets/img/logojeudi.png') }}"
                                 alt="logojeudi" class="py-0 my-0">
                        </div>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
<div id="footer">
    <div class="container">
        <div class="row py-1 align-items-center mt-4">
            <div class="col-md-11 col-sm-12">
            </div>
            <div class="col-1 d-none d-md-block">
                <img height="30px" src="{{ asset('assets/img/logosmall.png') }}" alt="logosmall"/>
            </div>
        </div>
    </div>
</div>
</body>
</html>